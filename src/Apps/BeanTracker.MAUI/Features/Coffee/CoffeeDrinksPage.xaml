<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="BeanTracker.MAUI.Features.Coffee.CoffeeDrinksPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:BeanTracker.MAUI.Features.Coffee"
    xmlns:core="clr-namespace:BeanTracker.Core.Coffee;assembly=BeanTracker.Core"
    xmlns:swipe="clr-namespace:Plugin.Maui.SwipeCardView;assembly=Plugin.Maui.SwipeCardView"
    x:DataType="vm:CoffeeDrinksViewModel"
    Title="Coffee Drinks">

    <Grid RowDefinitions="Auto,*">

        <!-- Top bar: Search + view-toggle button -->
        <Grid Grid.Row="0" ColumnDefinitions="*,Auto" Padding="0,4">
            <SearchBar
                Grid.Column="0"
                Placeholder="Search drinks…"
                Text="{Binding SearchQuery}"
                SearchCommand="{Binding SearchCommand}"
                IsVisible="{Binding IsListView}" />
            <Button
                Grid.Column="1"
                Text="{Binding ToggleViewLabel}"
                Command="{Binding ToggleViewCommand}"
                CornerRadius="20"
                FontSize="13"
                Padding="12,8"
                Margin="4,4,8,4"
                VerticalOptions="Center" />
        </Grid>

        <!-- ── List View ──────────────────────────────────────── -->
        <CollectionView
            Grid.Row="1"
            ItemsSource="{Binding Drinks}"
            IsVisible="{Binding IsListView}">
            <CollectionView.EmptyView>
                <Label
                    HorizontalOptions="Center"
                    VerticalOptions="Center"
                    Text="No drinks found." />
            </CollectionView.EmptyView>
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="core:CoffeeDrink">
                    <Grid ColumnDefinitions="64,*" Padding="16,10">
                        <Grid.GestureRecognizers>
                            <TapGestureRecognizer
                                Command="{Binding Source={RelativeSource AncestorType={x:Type vm:CoffeeDrinksViewModel}}, Path=SelectDrinkCommand}"
                                CommandParameter="{Binding .}" />
                        </Grid.GestureRecognizers>
                        <Border
                            BackgroundColor="{StaticResource Cappuccino}"
                            StrokeThickness="0"
                            StrokeShape="Ellipse"
                            HeightRequest="42" WidthRequest="42"
                            HorizontalOptions="Center" VerticalOptions="Center">
                            <Label
                                Text="☕"
                                FontSize="22"
                                HorizontalOptions="Center"
                                VerticalOptions="Center" />
                        </Border>
                        <VerticalStackLayout
                            Grid.Column="1"
                            VerticalOptions="Center"
                            Spacing="2">
                            <Label
                                Text="{Binding Name}"
                                FontSize="15"
                                FontAttributes="Bold" />
                            <Label
                                Text="{Binding Origin}"
                                FontSize="12"
                                TextColor="{StaticResource WarmGray400}" />
                        </VerticalStackLayout>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <!-- ── Card Swipe View ────────────────────────────────── -->
        <Grid Grid.Row="1" IsVisible="{Binding IsCardSwipeView}">

            <swipe:SwipeCardView
                x:Name="SwipeCard"
                ItemsSource="{Binding CardDrinks}"
                SupportedSwipeDirections="Left,Right"
                SupportedDraggingDirections="Left,Right"
                LoopCards="True"
                VerticalOptions="Fill"
                Margin="36,8">
            <swipe:SwipeCardView.ItemTemplate>
                <DataTemplate x:DataType="vm:CoffeeDrinkCardItem">
                    <Border
                        StrokeThickness="1"
                        Stroke="{StaticResource WarmGray200}"
                        BackgroundColor="{StaticResource FlatWhite}"
                        Padding="0">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="16" />
                        </Border.StrokeShape>

                        <Grid RowDefinitions="200,Auto,Auto,Auto,Auto,Auto">

                            <!-- Image -->
                            <Border
                                Grid.Row="0"
                                StrokeThickness="0"
                                HeightRequest="200">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="16,16,0,0" />
                                </Border.StrokeShape>
                                <Grid>
                                    <Image
                                        Source="{Binding ImageUrl}"
                                        Aspect="AspectFill"
                                        IsVisible="{Binding IsImageVisible}" />
                                    <ActivityIndicator
                                        IsRunning="{Binding IsImageLoading}"
                                        IsVisible="{Binding IsImageLoading}"
                                        Color="{StaticResource Cappuccino}"
                                        HorizontalOptions="Center"
                                        VerticalOptions="Center" />
                                </Grid>
                            </Border>

                            <!-- Name -->
                            <Label
                                Grid.Row="1"
                                Text="{Binding Drink.Name}"
                                FontSize="18"
                                FontAttributes="Bold"
                                TextColor="{StaticResource Espresso}"
                                Margin="16,12,16,0" />

                            <!-- Description -->
                            <Label
                                Grid.Row="2"
                                Text="{Binding Drink.Description}"
                                FontSize="13"
                                TextColor="{StaticResource WarmGray500}"
                                LineBreakMode="TailTruncation"
                                MaxLines="2"
                                Margin="16,4,16,0" />

                            <!-- Origin -->
                            <Grid Grid.Row="3" ColumnDefinitions="20,*" Margin="16,8,16,0" ColumnSpacing="6">
                                <Label Grid.Column="0" Text="🌍" FontSize="14" VerticalOptions="Center" />
                                <Label Grid.Column="1" Text="{Binding Drink.Origin}" FontSize="13" TextColor="{StaticResource WarmGray500}" VerticalOptions="Center" />
                            </Grid>

                            <!-- Flavor Notes -->
                            <Grid Grid.Row="4" ColumnDefinitions="20,*" Margin="16,4,16,0" ColumnSpacing="6">
                                <Label Grid.Column="0" Text="🍫" FontSize="14" VerticalOptions="Center" />
                                <Label
                                    Grid.Column="1"
                                    Text="{Binding FlavorNotesText}"
                                    FontSize="13"
                                    TextColor="{StaticResource WarmGray400}"
                                    LineBreakMode="TailTruncation"
                                    MaxLines="1"
                                    VerticalOptions="Center" />
                            </Grid>

                            <!-- Save to Favourites button -->
                            <Button
                                Grid.Row="5"
                                Text="{Binding FavouriteLabel}"
                                Command="{Binding ToggleFavouriteCommand}"
                                CommandParameter="{Binding .}"
                                BackgroundColor="{Binding FavouriteButtonBackground}"
                                TextColor="{Binding FavouriteButtonTextColor}"
                                CornerRadius="24"
                                Margin="16,8,16,16" />

                        </Grid>
                    </Border>
                </DataTemplate>
            </swipe:SwipeCardView.ItemTemplate>
            </swipe:SwipeCardView>

            <!-- Left swipe arrow -->
            <Label
                x:Name="LeftArrowHint"
                Text="❮"
                FontSize="36"
                FontAttributes="Bold"
                TextColor="{StaticResource Cappuccino}"
                HorizontalOptions="Start"
                VerticalOptions="Center"
                Margin="4,0,0,0"
                Opacity="0.85" />

            <!-- Right swipe arrow -->
            <Label
                x:Name="RightArrowHint"
                Text="❯"
                FontSize="36"
                FontAttributes="Bold"
                TextColor="{StaticResource Cappuccino}"
                HorizontalOptions="End"
                VerticalOptions="Center"
                Margin="0,0,4,0"
                Opacity="0.85" />

        </Grid>

    </Grid>

</ContentPage>
